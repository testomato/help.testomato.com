description: Common success message
content:
  application/json:
    schema:
      properties:
        message:
          type: string
        code:
          type: integer
          default: 200
        ok:
          type: boolean
          default: true
        error:
          type: boolean
          default: false
